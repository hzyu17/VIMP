"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class ack_t(object):
    __slots__ = ["msg_id", "timestamp", "src_channel"]

    def __init__(self):
        self.msg_id = 0
        self.timestamp = 0
        self.src_channel = ""

    def encode(self):
        buf = BytesIO()
        buf.write(ack_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">qq", self.msg_id, self.timestamp))
        __src_channel_encoded = self.src_channel.encode('utf-8')
        buf.write(struct.pack('>I', len(__src_channel_encoded)+1))
        buf.write(__src_channel_encoded)
        buf.write(b"\0")

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != ack_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return ack_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = ack_t()
        self.msg_id, self.timestamp = struct.unpack(">qq", buf.read(16))
        __src_channel_len = struct.unpack('>I', buf.read(4))[0]
        self.src_channel = buf.read(__src_channel_len)[:-1].decode('utf-8', 'replace')
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if ack_t in parents: return 0
        tmphash = (0x396a65844b536e3c) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if ack_t._packed_fingerprint is None:
            ack_t._packed_fingerprint = struct.pack(">Q", ack_t._get_hash_recursive([]))
        return ack_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

