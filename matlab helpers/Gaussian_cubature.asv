%% Gaussian Hermite cubature

%% One dimension
syms x
gx = x^2;
Int = GaussHermitOneDim(gx, 6, 1, 1.2);

%% Higher dimensions
n = 2;
m = zeros(n, 1);
P = eye(n);

p = 8; % Hermite polynomial degree
x = sym('x', [n 1]);
gx = x(1)^2 + x(2)^2
sig = sqrtm(P);
gaussian_x = det(2*pi*P)^(-1/2) * expm(-transpose(x-m)*inv(P)*(x-m)/2);

func_hd = matlabFunction(gx*gaussi)
